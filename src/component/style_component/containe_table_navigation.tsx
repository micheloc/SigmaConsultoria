import { StyleSheet } from 'react-native';

const sTableNavigation = StyleSheet.create({
  body: {
    backgroundColor: '#12994a',
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default sTableNavigation;
